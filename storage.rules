
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite la lectura pública de todos los archivos
    match /{allPaths=**} {
      allow read: if true;
    }

    // Permite la escritura en la carpeta 'brands' para cualquier usuario (para logos)
    match /brands/{entityId}/{fileName} {
      allow write: if true;
    }

    // Permite la escritura en la carpeta 'cars' solo si el usuario está autenticado
    match /cars/{entityId}/{fileName} {
      allow write: if request.auth != null;
    }
  }
}
